[build-system]
requires = ["scikit-build-core>=0.10", "pybind11"]
build-backend = "scikit_build_core.build"

[project]
name = "pytick"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy"]